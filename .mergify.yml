pull_request_rules:
  - name: automatic merge on CI success
    conditions:
      - check-success=tests (ubuntu-latest)
      - author=nix-eval-jobs-bot
    actions:
      merge:
        method: merge
      delete_head_branch: {}
